(defpackage usuage-test
  (:use :cl
        :usuage
        :prove))
(in-package :usuage-test)

;; NOTE: To run this test file, execute `(asdf:test-system :usuage)' in your Lisp.

(plan nil)

;; blah blah blah.

(finalize)
